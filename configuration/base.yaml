application:
  port: 8000
database:
  port: 5432
  username: "postgres"
  password: "password"
  database_name: "newsletter"
  require_ssl: false
email_client:
  base_url: "https://api.postmarkapp.com"
  # "Single sender email" authorised on Postmark
  sender_email: "hazadus@hazadus.ru"
  authorization_token: "auth-token"
  timeout_milliseconds: 10000